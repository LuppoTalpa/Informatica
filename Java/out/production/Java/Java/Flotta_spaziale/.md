## 🚀 Esercizio: Gestione Flotta Spaziale "Astra"

### Contesto
Sei un ingegnere software per la corporazione "Astra Logistics" e devi sviluppare il sistema di gestione della flotta. Il sistema deve catalogare diverse navi spaziali, gestire le missioni di trasporto e assegnare la nave "ottimale" a ciascuna missione, bilanciando capacità, sicurezza e costi operativi.

### Obiettivi Didattici
- **Ereditarietà e Classi Astratte:** Definire una `NaveSpaziale` generica e specializzarla in `NaveDaCarico` e `IncrociatoreDaBattaglia`.
- **Composizione:** Una `Missione` contiene un `ArrayList` di `Container`. Una `StazioneSpaziale` contiene un `ArrayList` di `NaveSpaziale`.
- **ArrayList:** Usare `ArrayList` per la flotta e per il carico delle missioni.
- **Metodi Complessi:** Implementare una logica di *ottimizzazione* per trovare la nave migliore per una missione, usando il polimorfismo.

---

### 📦 Classe 1: Container (Classe Semplice)
Una semplice classe POJO che rappresenta un'unità di carico.

**Attributi:**
- `String idContainer`
- `double pesoTonnellate`
- `boolean materialePericoloso` (se `true`, richiede una nave più sicura)

**Metodi:**
- Costruttore e Getters.

---

### 📜 Classe 2: Missione (Composizione)
Rappresenta un contratto di trasporto. Ha una lista di container.

**Attributi:**
- `String idMissione`
- `String destinazione` (es. "Settore Kepler-186f")
- `int livelloPericoloSettore` (1–10)
- `ArrayList<Container> caricoDellaMissione`

**Metodi:**
- Costruttore  
- `void aggiungiContainer(Container c)`  
- `double getPesoTotale()`: somma i pesi di tutti i container  
- `boolean isMissionePericolosa()`: ritorna `true` se `livelloPericoloSettore > 5` o se almeno un container è pericoloso

---

### 🛰️ Classe 3: NaveSpaziale (Astratta)
Rappresenta una nave generica della flotta. Non può essere istanziata.

**Attributi:**
- `String idRegistrazione`
- `double capacitaMassimaStiva`
- `StatoNave stato`

**Metodi:**
- Costruttore e Getters  
- `void setStato(StatoNave nuovoStato)`  
- `boolean puoTrasportare(double pesoRichiesto)`  
- `abstract boolean requisitiSicurezzaSoddisfatti(Missione m)`  
- `abstract double calcolaCostoOperativo(Missione m)`

#### Sotto-compito 1: Enum `StatoNave`
```java
DISPONIBILE,
IN_MISSIONE,
IN_MANUTENZIONE
````

---

### 🚚 Classe 4: NaveDaCarico (Concreta)

Specializzata per il trasporto sicuro, non per il combattimento.

**Eredita da:** `NaveSpaziale`

**Attributi Aggiuntivi:**

* `boolean stivaSchermata`

**Metodi:**

* Costruttore (`super()`)
* `requisitiSicurezzaSoddisfatti(Missione m)`:

    * `false` se `m.getLivelloPericoloSettore() > 5`
    * `false` se `m.isMissionePericolosa()` e `stivaSchermata == false`
    * `true` altrimenti
* `calcolaCostoOperativo(Missione m)`:

    * `10 * m.getPesoTotale()`

---

### 💥 Classe 5: IncrociatoreDaBattaglia (Concreta)

Veloce, armata, costosa. Può trasportare carico, ma non è il suo scopo primario.

**Eredita da:** `NaveSpaziale`

**Attributi Aggiuntivi:**

* `int potenzaScudi`
* `int numeroCannoniLaser`

**Metodi:**

* Costruttore (`super()`)
* `requisitiSicurezzaSoddisfatti(Missione m)`:

    * `false` se `potenzaScudi < m.getLivelloPericoloSettore()`
    * ignora `materialePericoloso`
    * `true` altrimenti
* `calcolaCostoOperativo(Missione m)`:

    * `5000 + (10 * m.getPesoTotale() * 1.5)`

---

### 🌌 Classe 6: StazioneSpaziale (Principale)

Gestisce la flotta e le missioni.

**Attributi:**

* `String nomeStazione`
* `ArrayList<NaveSpaziale> flottaDisponibile`
* `ArrayList<Missione> missioniInAttesa`

**Metodi:**

* Costruttore
* `void aggiungiNaveAFlotta(NaveSpaziale n)`
* `void aggiungiMissione(Missione m)`

#### 🧠 Metodo Complesso da Implementare

```java
public NaveSpaziale assegnaNaveOttimale(Missione missione)
```

**Logica:**

1. Calcola `pesoTotale`.
2. Crea `ArrayList<NaveSpaziale> naviCandidate`.
3. Itera su `flottaDisponibile`:

    * ignora se `stato != DISPONIBILE`
    * ignora se `!puoTrasportare(pesoTotale)`
    * ignora se `!requisitiSicurezzaSoddisfatti(missione)`
    * altrimenti aggiungi a `naviCandidate`
4. Se nessuna candidata → stampa messaggio e ritorna `null`
5. Altrimenti → trova la nave col minor costo (`calcolaCostoOperativo`)
6. Imposta la nave trovata su `IN_MISSIONE`
7. Rimuovi la missione da `missioniInAttesa`
8. Restituisci la nave scelta

---

### 🚀 Compito

1. Implementa tutte le classi e l’enum `StatoNave`.
2. Scrivi i metodi di calcolo (`getPesoTotale`, `isMissionePericolosa`).
3. Implementa la logica di `assegnaNaveOttimale`.
4. Crea una classe `Main` per testare:

    * Crea una `StazioneSpaziale`.
    * Crea 2 `NaveDaCarico` (una schermata e una no) e 1 `IncrociatoreDaBattaglia`.
    * Crea 3 missioni:

        * facile → poco peso, pericolo 2
        * pericolosa → poco peso, pericolo 8
        * speciale → peso medio, pericolo 2, carico pericoloso
    * Assegna le missioni e stampa l’ID della nave scelta.
